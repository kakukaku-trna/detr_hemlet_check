# 🚀 训练参数配置指南

**最后更新**: 2026-06-10  
**适用版本**: Deformable DETR with ConvNeXt Backbone

---

## 📋 当前系统默认参数

### 基础参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--lr` | 2e-4 (0.0002) | 主学习率 |
| `--lr_backbone` | 2e-5 (0.00002) | Backbone 学习率 (10x 降低) |
| `--lr_linear_proj_mult` | 0.1 | 线性投影层的学习率倍数 |
| `--batch_size` | 2 | 批大小 |
| `--weight_decay` | 1e-4 (0.0001) | L2 正则化 |
| `--epochs` | 400 | 默认训练轮次 |
| `--lr_drop` | 120 | 学习率衰减轮次 |
| `--clip_max_norm` | 0.1 | 梯度裁剪最大值 |

### Backbone 参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--backbone` | 'resnet50' | Backbone 类型 |
| `--dilation` | False | 是否在最后一个卷积块使用空洞卷积 |
| `--position_embedding` | 'sine' | 位置编码类型 (sine/learned) |
| `--num_feature_levels` | 4 | 特征金字塔层数 |

### Transformer 参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--enc_layers` | 6 | Encoder 层数 |
| `--dec_layers` | 6 | Decoder 层数 |
| `--hidden_dim` | 256 | Transformer 隐层维度 |
| `--nheads` | 8 | 多头注意力头数 |
| `--dim_feedforward` | 1024 | FFN 中间维度 |
| `--dropout` | 0.1 | Dropout 比例 |
| `--num_queries` | 300 | 查询对象数 |
| `--enc_n_points` | 4 | Encoder 采样点数 |
| `--dec_n_points` | 4 | Decoder 采样点数 |

### 数据集参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--dataset_file` | 'coco' | 数据集类型 |
| `--coco_path` | './data/coco' | COCO 数据路径 |
| `--num_classes` | 3 | 类别数 (已修改) |
| `--num_workers` | 2 | 数据加载线程数 |

---

## 🎯 方案B 训练配置

### 基线训练（Baseline）

**目标**: 建立 ConvNeXt-Tiny 的基线性能，看 mAP 降低多少

```bash
# 方案 B.1: 基线训练 - 简洁版
python main.py \
  --backbone convnext_tiny \
  --num_decoder_layers 4 \
  --num_queries 150 \
  --enc_n_points 2 \
  --dec_n_points 2 \
  --epochs 50 \
  --output_dir ./checkpoints/convnext_baseline
```

**完整参数版本**:

```bash
# 方案 B.2: 基线训练 - 完整版
python main.py \
  --backbone convnext_tiny \
  --num_decoder_layers 4 \
  --enc_layers 6 \
  --num_queries 150 \
  --enc_n_points 2 \
  --dec_n_points 2 \
  --lr 2e-4 \
  --lr_backbone 2e-5 \
  --batch_size 2 \
  --epochs 50 \
  --lr_drop 15 \
  --weight_decay 1e-4 \
  --num_workers 4 \
  --device cuda \
  --num_classes 3 \
  --output_dir ./checkpoints/convnext_baseline
```
python main.py --backbone convnext_tiny --dec_layers 4 --enc_layers 6 --num_queries 150 --enc_n_points 2 --dec_n_points 2 --lr 2e-4 --lr_backbone 2e-5 --batch_size 2 --epochs 50 --lr_drop 15 --weight_decay 1e-4 --num_workers 4 --device cuda --num_classes 3 --output_dir ./checkpoints/convnext_baseline --dataset_file coco --coco_path /home/jiangyang.li2/detr_hemlet_check/shujuji

**参数说明**:

| 参数 | 值 | 原因 |
|------|-----|------|
| `--backbone convnext_tiny` | ✅ | 使用 ConvNeXt-Tiny Backbone |
| `--num_decoder_layers 4` | ✅ | 从 6 → 4 (减少 33%) |
| `--num_queries 150` | ✅ | 从 300 → 150 (减少 50%) |
| `--enc_n_points 2` | ✅ | 从 4 → 2 (减少 50%) |
| `--dec_n_points 2` | ✅ | 从 4 → 2 (减少 50%) |
| `--epochs 50` | ✅ | 比默认 400 少，用于快速验证 |
| `--lr_drop 15` | ✅ | 15 个 epoch 后衰减 (50 的 30%) |

**预期结果**:
- Baseline mAP: ~40.5 (-2.0 from ResNet50)
- 训练时间: 3-4 天 (GPU)
- 延迟: ~20-25ms
- FPS: ~40+

**检查点**:
- 保存在 `./checkpoints/convnext_baseline/`
- 最佳模型: `./checkpoints/convnext_baseline/checkpoint_best.pth`

---

### 蒸馏训练（Distillation）

**目标**: 通过知识蒸馏恢复精度到 -1.3 (or better)

```bash
# 方案 B.3: 蒸馏训练 - 简洁版
python main.py \
  --backbone convnext_tiny \
  --num_decoder_layers 4 \
  --num_queries 150 \
  --enc_n_points 2 \
  --dec_n_points 2 \
  --distillation True \
  --teacher_checkpoint ./checkpoints/resnet50_original/checkpoint_best.pth \
  --epochs 50 \
  --output_dir ./checkpoints/convnext_distilled
```

**完整参数版本**:

```bash
# 方案 B.4: 蒸馏训练 - 完整版
python main.py \
  --backbone convnext_tiny \
  --num_decoder_layers 4 \
  --enc_layers 6 \
  --num_queries 150 \
  --enc_n_points 2 \
  --dec_n_points 2 \
  --distillation True \
  --teacher_checkpoint ./checkpoints/resnet50_original/checkpoint_best.pth \
  --distillation_alpha 0.5 \
  --distillation_temp 4.0 \
  --lr 2e-4 \
  --lr_backbone 2e-5 \
  --batch_size 2 \
  --epochs 50 \
  --lr_drop 15 \
  --weight_decay 1e-4 \
  --num_workers 4 \
  --device cuda \
  --num_classes 3 \
  --output_dir ./checkpoints/convnext_distilled
```

**蒸馏参数说明**:

| 参数 | 值 | 说明 |
|------|-----|------|
| `--distillation True` | ✅ | 启用知识蒸馏 |
| `--teacher_checkpoint` | 必需 | Teacher 模型路径 (原始 ResNet50) |
| `--distillation_alpha` | 0.5 | 蒸馏损失权重 (0.5 = 50% KD + 50% CE) |
| `--distillation_temp` | 4.0 | 蒸馏温度 (控制软标签的平滑度) |

**蒸馏参数推荐**:

```
distillation_alpha (蒸馏权重):
  • 0.3  → 更重视原始标签 (快速收敛，保留原始学习)
  • 0.5  → 平衡 (推荐，普遍效果好)
  • 0.7  → 更重视蒸馏 (慢速收敛，精度可能更高)

distillation_temp (蒸馏温度):
  • 3.0  → 比较硬的软标签 (快速收敛)
  • 4.0  → 中等软标签 (推荐，平衡收敛和效果)
  • 5.0+ → 比较软的软标签 (慢收敛，可能更平滑)
```

**预期结果**:
- Distilled mAP: ~41.2 (-1.3 from ResNet50)
- 精度恢复: +0.7 (from baseline -2.0 to -1.3)
- 训练时间: 3-4 天 (GPU)
- 延迟: ~20-25ms (不变)
- FPS: ~40+ (不变)

**检查点**:
- 保存在 `./checkpoints/convnext_distilled/`
- 最佳模型: `./checkpoints/convnext_distilled/checkpoint_best.pth`

---

## 📊 参数对比表

### ResNet50 原始 vs ConvNeXt-Tiny 方案B

| 参数 | ResNet50 原始 | ConvNeXt-Tiny 方案B | 变化 |
|------|-------------|-------------------|------|
| Backbone | resnet50 | convnext_tiny | ✅ |
| Decoder 层数 | 6 | 4 | -33% |
| Query 数 | 300 | 150 | -50% |
| Encoder 采样点 | 4 | 2 | -50% |
| Decoder 采样点 | 4 | 2 | -50% |
| 学习率 | 2e-4 | 2e-4 | 不变 |
| Batch Size | 2 | 2 | 不变 |
| 训练轮次 | 50 | 50 | 相同 |

---

## ⚙️ 运行前检查清单

### 必须完成

- [ ] ConvNeXt Backbone 代码已实现 (`models/backbone.py`)
- [ ] 前向传播已验证 (`test_convnext_simple.py` ✅)
- [ ] GPU 内存充足 (建议 ≥ 8GB)
- [ ] COCO 数据集已下载并放在 `./data/coco/`
- [ ] Teacher 模型已准备 (ResNet50 baseline checkpoint)

### 可选优化

- [ ] 增加 `--num_workers` 到 4-8 (加快数据加载)
- [ ] 如果 GPU 内存充足，可提高 `--batch_size` 到 4-8
- [ ] 如果想要更好的精度，可增加 `--epochs` 到 100-150

---

## 📈 性能监控

### 训练期间监控

```bash
# 查看训练进度
tail -f checkpoints/convnext_baseline/log.txt

# GPU 监控
watch -n 1 nvidia-smi

# 实时 TensorBoard
tensorboard --logdir checkpoints/convnext_baseline --port 6006
```

### 关键指标

**基线训练**:
- mAP: 应该从 ~40 (初始) 上升到 ~40.5
- Loss: 应该持续下降
- 训练时间: 每个 epoch ~15-20 分钟

**蒸馏训练**:
- Student mAP: 应该从 ~40.5 上升到 ~41.2
- KD Loss: 应该在 Baseline 基础上额外贡献
- 收敛速度: 应该比 Baseline 快

---

## 🔍 常见问题

### Q1: 为什么 Backbone 要用 convnext_tiny？

**A**: 因为:
- 参数少 (-36%)
- 延迟低 (-20%)
- 而且根据实测，这个配置是平衡的

---

### Q2: 为什么 Decoder 层数从 6 降到 4？

**A**: 为了进一步减少参数和计算量:
- 完整 DETR 参数中，Decoder 占比 20-30%
- 从 6 层 → 4 层可减少 33% Decoder 参数

---

### Q3: 为什么采样点从 4 降到 2？

**A**: 因为：
- Deformable Attention 的计算瓶颈
- 采样点越多，计算越多
- 从 4 → 2 是平衡的折中

---

### Q4: 如果 mAP 降低超过 -2.0 怎么办？

**A**: 说明 ConvNeXt-Tiny 对你的数据不适合。可以:
1. 尝试 ConvNeXt-Small (参数更多)
2. 增加蒸馏强度 (提高 `--distillation_alpha`)
3. 增加训练轮次 (提高 `--epochs` 到 100)

---

### Q5: 蒸馏需要 Teacher 模型吗？

**A**: 是的，需要提供:
- ResNet50 原始配置的最佳 checkpoint
- 或者从头训练一个 ResNet50 baseline

推荐使用原始的、已经训练好的 ResNet50 模型。

---

## 🚀 快速启动

### 一键启动基线训练

```bash
cd /home/jiangyang.li2/detr_hemlet_check/Deformable-DETR

# 创建输出目录
mkdir -p checkpoints/convnext_baseline

# 启动训练
python main.py \
  --backbone convnext_tiny \
  --num_decoder_layers 4 \
  --num_queries 150 \
  --enc_n_points 2 \
  --dec_n_points 2 \
  --epochs 50 \
  --output_dir ./checkpoints/convnext_baseline
```

### 一键启动蒸馏训练

```bash
# 创建输出目录
mkdir -p checkpoints/convnext_distilled

# 启动训练
python main.py \
  --backbone convnext_tiny \
  --num_decoder_layers 4 \
  --num_queries 150 \
  --enc_n_points 2 \
  --dec_n_points 2 \
  --distillation True \
  --teacher_checkpoint ./checkpoints/resnet50_original/checkpoint_best.pth \
  --epochs 50 \
  --output_dir ./checkpoints/convnext_distilled
```

---

## 📝 保存最佳结果

训练完成后，最佳模型会保存在:
- 基线: `./checkpoints/convnext_baseline/checkpoint_best.pth`
- 蒸馏: `./checkpoints/convnext_distilled/checkpoint_best.pth`

**评估最佳模型**:

```bash
# 评估基线
python main.py \
  --backbone convnext_tiny \
  --num_decoder_layers 4 \
  --num_queries 150 \
  --enc_n_points 2 \
  --dec_n_points 2 \
  --resume ./checkpoints/convnext_baseline/checkpoint_best.pth \
  --eval

# 评估蒸馏
python main.py \
  --backbone convnext_tiny \
  --num_decoder_layers 4 \
  --num_queries 150 \
  --enc_n_points 2 \
  --dec_n_points 2 \
  --resume ./checkpoints/convnext_distilled/checkpoint_best.pth \
  --eval
```

---

## 💡 建议

### 针对你的场景

基于实测数据和 mAP 不会大幅下降的目标：

1. **立即启动基线训练**
   - 使用简洁版命令 (方案 B.1)
   - 3-4 天内完成
   - 获取准确的 mAP 数据

2. **根据结果决定蒸馏**
   - 如果 Baseline mAP = -2.0: 立即蒸馏
   - 如果 Baseline mAP = -2.5+: 考虑调整参数后重新训练

3. **蒸馏训练**
   - 使用推荐的 `distillation_alpha=0.5, temp=4.0`
   - 3-4 天完成
   - 验证精度恢复到 -1.3

---

**祝训练顺利！** 🚀

有问题随时问我。

